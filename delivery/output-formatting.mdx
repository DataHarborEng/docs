---
title: Output Formatting
description: Convert Virtual API responses to Markdown, CSV, or YAML — via spec config, URL extension, or Accept header content negotiation.
---

# Output Formatting

**JSON in, any format out. Zero client code.**

Virtual API responses are JSON by default. Output formatting lets you convert responses to Markdown, CSV, or YAML — configured in your Virtual API Configuration, requested via URL extension, or negotiated through the HTTP `Accept` header. Especially useful for AI agents that present data to humans, or for feeding governed data directly into spreadsheets and configuration tools.

<Info>
  Output formatting runs **after** all privacy controls and data transforms. Your controls pipeline is unchanged — formatting just converts the final result.
</Info>

## Quick start

Add `default_output_format` to any Virtual API Configuration:

```yaml
version: "0.3"
default_output_format: markdown
objects:
  customers:
    controls:
      - type: redact
        fields: [ssn]
      - type: tokenize
        fields: [email]
```

Every response from this Virtual API is now Markdown instead of JSON.

## Configuration

Two optional top-level fields control formatting:

| Field | Values | Default | Description |
|-------|--------|---------|-------------|
| `input_format` | `json`, `csv`, `yaml`, `markdown` | Auto-detected from upstream `Content-Type` | Declare the expected upstream format |
| `default_output_format` | `json`, `markdown`, `csv`, `yaml` | *(omitted — preserve input format)* | Declare the desired response format |

Both fields are completely optional. Omitting them preserves the current behavior — JSON in, JSON out.

```yaml
version: "0.3"
input_format: json
default_output_format: markdown
objects:
  _default:
    controls:
      - type: allow
        fields: [name, description, status]
```

<Tip>
  `input_format` is rarely needed. DataHarbor auto-detects the upstream format from the `Content-Type` header (`application/json`, `text/csv`, `text/yaml`, `application/yaml`, `text/markdown`). Only set it when the upstream API returns a non-standard content type that doesn't match the actual payload format.
</Tip>

### Setting `default_output_format: json`

You can explicitly set `default_output_format: json` to signal that the response should remain JSON. This is a passthrough — no conversion happens. It's useful for documentation clarity or when you want to be explicit about the contract.

## URL extension format

Append a format extension to any data endpoint URL to request a specific output format without changing the Virtual API Configuration:

```bash
# Markdown output
curl -H "dataHarbor-api-key: YOUR_API_KEY" \
  https://service.dataharbor.co/fetch/b097a4d3/customers.md

# CSV output
curl -H "dataHarbor-api-key: YOUR_API_KEY" \
  https://service.dataharbor.co/fetch/b097a4d3/customers.csv

# YAML output
curl -H "dataHarbor-api-key: YOUR_API_KEY" \
  https://service.dataharbor.co/fetch/b097a4d3/customers.yaml

# Explicit JSON output
curl -H "dataHarbor-api-key: YOUR_API_KEY" \
  https://service.dataharbor.co/fetch/b097a4d3/customers.json
```

This works on both `/fetch/` and `/relay/` endpoints.

## Accept header negotiation

DataHarbor supports HTTP content negotiation via the `Accept` header per [RFC 9110](https://httpwg.org/specs/rfc9110.html#field.accept). Clients can request a specific output format by including the appropriate media type:

| Accept value | Output format |
|---|---|
| `application/json` | JSON |
| `text/markdown` | Markdown |
| `text/csv` | CSV |
| `text/yaml` | YAML |

```bash
# Request Markdown via Accept header
curl -H "dataHarbor-api-key: YOUR_API_KEY" \
  -H "Accept: text/markdown" \
  https://service.dataharbor.co/fetch/b097a4d3/customers

# Request CSV via Accept header
curl -H "dataHarbor-api-key: YOUR_API_KEY" \
  -H "Accept: text/csv" \
  https://service.dataharbor.co/fetch/b097a4d3/customers

# Request YAML via Accept header
curl -H "dataHarbor-api-key: YOUR_API_KEY" \
  -H "Accept: text/yaml" \
  https://service.dataharbor.co/fetch/b097a4d3/customers
```

### AI agents and Accept headers

This is especially useful for AI agents, which typically send `Accept` headers expressing their format preferences. DataHarbor respects these automatically:

| Agent | Accept header sent |
|---|---|
| Claude Code | `Accept: text/markdown, text/html, */*` |
| Cursor | `Accept: text/markdown, text/plain;q=0.9, */*;q=0.8` |

Because these agents prefer `text/markdown`, DataHarbor returns Markdown without any spec or URL configuration — the agent gets the format it wants out of the box.

### Quality values (q-values)

The `Accept` header supports quality values (`q`) to express preference. Values range from `0` to `1`, where `1.0` is the default when omitted and `q=0` means "never send this format":

```
Accept: text/markdown, text/csv;q=0.5, application/json;q=0.2
```

DataHarbor picks the supported format with the highest q-value. In this example: Markdown (`q=1.0` implicit) > CSV (`q=0.5`) > JSON (`q=0.2`).

### Error handling

When the `Accept` header explicitly excludes all supported formats and contains no wildcard fallback, DataHarbor returns `406 Not Acceptable`. This is rare in practice — most clients include `*/*` as a fallback.

<Info>
  When Accept is `*/*` or missing, DataHarbor falls through to the spec default or upstream format — no 406.
</Info>

### Caching

DataHarbor always includes `Vary: Accept` in the response headers so that caches and CDNs store separate entries per Accept value.

### Works through MCP

Accept header negotiation works through MCProxy and MCP delivery. When an AI agent connects via MCP, its `Accept` header is forwarded to the lease endpoint, so the agent's format preference is respected end-to-end.

## Precedence

Format selection follows a four-tier precedence (highest to lowest):

| Priority | Source | Example |
|---|---|---|
| 1 | URL extension | `.md`, `.csv`, `.yaml`, `.json` |
| 2 | Accept header | `Accept: text/markdown` |
| 3 | Spec default | `default_output_format: markdown` |
| 4 | Upstream format | Whatever the upstream API returns |

The first match wins. Some examples:

| `default_output_format` | URL extension | Accept header | Result |
|---|---|---|---|
| `markdown` | `.json` | `text/csv` | **JSON** — URL extension wins |
| `json` | *(none)* | `text/markdown` | **Markdown** — Accept header wins over spec |
| `markdown` | *(none)* | *(none)* | **Markdown** — spec default applies |
| *(omitted)* | *(none)* | `text/csv` | **CSV** — Accept header applies |
| *(omitted)* | `.md` | `text/csv` | **Markdown** — URL extension wins |
| *(omitted)* | *(none)* | *(none)* | **JSON** — no formatting |

## Markdown rendering

DataHarbor uses **structure-driven rendering** — it inspects the shape of the JSON and picks the best Markdown representation automatically.

### Array of objects → Table

```json
[
  { "name": "Alice", "role": "Admin", "status": "active" },
  { "name": "Bob", "role": "Viewer", "status": "inactive" }
]
```

```markdown
| name | role | status |
|---|---|---|
| Alice | Admin | active |
| Bob | Viewer | inactive |
```

### Single object → Key-value list

```json
{ "name": "Alice", "email": "tok_abc123", "role": "Admin" }
```

```markdown
**name:** Alice
**email:** tok_abc123
**role:** Admin
```

### Nested object → Headers + structure

```json
{
  "user": {
    "name": "Alice",
    "address": { "city": "Portland", "state": "OR" }
  },
  "orders": [
    { "id": 1, "total": 25.00 },
    { "id": 2, "total": 42.50 }
  ]
}
```

```markdown
## user

**name:** Alice

### address

**city:** Portland
**state:** OR

## orders

| id | total |
|---|---|
| 1 | 25 |
| 2 | 42.5 |
```

### Rendering rules

| JSON shape | Markdown representation |
|---|---|
| Array of flat objects | Table with column headers from keys |
| Single flat object | Key-value list |
| Nested object | Hierarchical headers + nested content |
| Array of scalars | Bulleted list |
| Scalar value | Plain text |

## CSV rendering

CSV output follows [RFC 4180](https://datatracker.ietf.org/doc/html/rfc4180). Ideal for importing governed data into spreadsheets, data warehouses, or BI tools.

### Array of objects → CSV table

```json
[
  { "name": "Alice", "role": "Admin", "status": "active" },
  { "name": "Bob", "role": "Viewer", "status": "inactive" }
]
```

```csv
name,role,status
Alice,Admin,active
Bob,Viewer,inactive
```

### Single object → Header + row

```json
{ "name": "Alice", "email": "tok_abc123", "role": "Admin" }
```

```csv
name,email,role
Alice,tok_abc123,Admin
```

### Rendering rules

| JSON shape | CSV representation |
|---|---|
| Array of flat objects | Header row from union of all keys + data rows |
| Single flat object | Header row + single data row |
| Nested objects/arrays | Serialized as JSON strings in the cell |
| Null values | Empty cell |
| Scalar value | Plain text |

Values containing commas, quotes, or newlines are automatically quoted per RFC 4180. Quotes inside values are escaped by doubling (`""`).

<Info>
  Objects with different key sets are handled gracefully — the header row contains the union of all keys, and missing values render as empty cells.
</Info>

## YAML rendering

YAML output uses standard [YAML 1.2](https://yaml.org/spec/1.2.2/) formatting. Useful for configuration pipelines, infrastructure-as-code workflows, or agents that consume structured data in a human-readable format.

### Object → Key-value pairs

```json
{ "name": "Alice", "email": "tok_abc123", "role": "Admin" }
```

```yaml
name: Alice
email: tok_abc123
role: Admin
```

### Nested object → Indented structure

```json
{
  "user": {
    "name": "Alice",
    "address": { "city": "Portland", "state": "OR" }
  }
}
```

```yaml
user:
  name: Alice
  address:
    city: Portland
    state: OR
```

### Array of objects → Sequence of mappings

```json
[
  { "name": "Alice", "role": "Admin" },
  { "name": "Bob", "role": "Viewer" }
]
```

```yaml
- name: Alice
  role: Admin
- name: Bob
  role: Viewer
```

### Rendering rules

| JSON shape | YAML representation |
|---|---|
| Object | `key: value` pairs, nested objects indented 2 spaces |
| Array of objects | `- key: value` block sequence |
| Array of scalars | `- item` list |
| Strings with special chars | Quoted with escape sequences |
| Numbers and booleans | Unquoted |
| Null | `null` |

## Response headers

The response `Content-Type` changes based on the output format:

| Output format | Content-Type |
|---|---|
| JSON (default) | `application/json` |
| Markdown | `text/markdown; charset=utf-8` |
| CSV | `text/csv; charset=utf-8` |
| YAML | `text/yaml; charset=utf-8` |

`Vary: Accept` is always included in responses so that caches correctly differentiate responses served via content negotiation.

## Works everywhere

Output formatting is applied in the data pipeline, so every delivery mechanism benefits automatically:

- **REST API** — `/fetch/` and `/relay/` endpoints return formatted responses
- **MCP Server** — AI agents receive Markdown directly from their data tools
- **URL extensions** — Append `.md` for quick Markdown access from `curl` or scripts
- **Accept header** — Clients negotiate their preferred format automatically via HTTP content negotiation

## Input normalization

DataHarbor's controls pipeline operates on a canonical JSON model internally. When the upstream API returns a non-JSON format, DataHarbor automatically normalizes it to JSON before controls run, then converts to your requested output format.

```
Upstream API → Normalize to JSON → Apply Controls → Output Format → Respond
```

This means you can enroll a CSV, YAML, or Markdown API, apply the same privacy controls and transforms, and deliver the result in any output format.

### Supported input formats

| Format | Content-Type | Normalized shape |
|--------|-------------|-----------------|
| JSON | `application/json`, `application/*+json` | Parsed directly — no conversion |
| CSV | `text/csv` | Array of objects, one per row, keyed by header columns |
| YAML | `text/yaml`, `application/yaml`, `application/x-yaml` | JSON-compatible subset: mappings, sequences, scalars |
| Markdown | `text/markdown` | Document object with `content` and optional `frontmatter` |

### CSV normalization

CSV input is parsed as headered rectangular data. The first row defines the column names, and each subsequent row becomes a JSON object:

```csv
name,age,active
Alice,30,true
Bob,25,false
```

Normalizes to:

```json
[
  { "name": "Alice", "age": 30, "active": true },
  { "name": "Bob", "age": 25, "active": false }
]
```

Type coercion is conservative: obvious booleans (`true`/`false`), integers, and decimals are converted. Everything else — dates, GUIDs, leading-zero identifiers — stays as a string.

### YAML normalization

YAML input is normalized using the JSON-compatible subset of YAML: mappings become objects, sequences become arrays, and scalars are coerced to strings, numbers, booleans, or null.

```yaml
name: Alice
tags:
  - admin
  - owner
active: true
```

Normalizes to:

```json
{
  "name": "Alice",
  "tags": ["admin", "owner"],
  "active": true
}
```

<Warning>
  Advanced YAML constructs that don't map to JSON — anchors/aliases, tags, merge keys, multi-document streams, and non-finite floats (Infinity, NaN) — are rejected with a descriptive error. Only the JSON-compatible subset of YAML is supported.
</Warning>

### Markdown normalization

Markdown is treated as a **document**, not structured data. The raw Markdown body is preserved as-is in a `content` field. When YAML front matter is present (a `---`-fenced block at the very start of the document), it is parsed into a `frontmatter` object.

````markdown
---
title: API Guide
tags:
  - rest
  - auth
draft: false
---
# API Guide

This guide covers authentication...
````

Normalizes to:

```json
{
  "frontmatter": {
    "title": "API Guide",
    "tags": ["rest", "auth"],
    "draft": false
  },
  "content": "# API Guide\n\nThis guide covers authentication..."
}
```

When no front matter is present, the result is simply:

```json
{
  "content": "# Hello World\n\nSome markdown content."
}
```

Your controls can then target `content` or specific front matter fields like `frontmatter.title`.

#### Front matter rules

- Front matter must start at the very first line of the document with `---`
- The closing fence can be `---` or `...`
- The front matter block must be a YAML mapping (key-value pairs) — bare scalars or sequences are ignored
- Malformed YAML in the front matter block is silently ignored — the document normalizes with `content` only
- Horizontal rules (`---`) appearing later in the document body are not treated as front matter

<Info>
  Markdown normalization is document-oriented by design. DataHarbor does not infer structure from headings, lists, or tables — the raw Markdown body is preserved exactly as received.
</Info>

### Body sniffing

When the upstream `Content-Type` header is missing or unrecognized and no `input_format` is declared, DataHarbor inspects the first 512 bytes of the response body to detect the format:

- Starts with `{` or `[` → JSON
- Two rows with matching comma-separated field counts → CSV
- Starts with `---` or a `key:` mapping → YAML
- Starts with `#` → Markdown

If detection is inconclusive, DataHarbor defaults to JSON.

## Pipeline position

Formatting runs as the **last step** before the response is returned:

```
Upstream API → Authenticate → Authorize → Fetch → Normalize → Apply Controls → Output Format → Respond
```

Your privacy controls and data transforms execute on the normalized JSON model. The formatted output reflects the fully governed, transformed data. When the upstream is already JSON, normalization is a zero-cost passthrough.

## Next steps

<CardGroup cols={2}>
  <Card title="MCP Server Delivery" icon="robot" href="/delivery/mcp-server">
    AI agent access with governed data
  </Card>
  <Card title="Data Control" icon="shield" href="/control-blocks/data-control">
    Field-level privacy controls
  </Card>
</CardGroup>
